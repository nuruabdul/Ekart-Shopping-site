import { Component } from '@angular/core';

@Component({
  // selector: '[app-topheader]',   Used as a html attribute
  // selector:'#app-topheader' ,    //Using selector as a css class
  selector:'app-topheader',    //Using selector as html attribute
  templateUrl: './topheader.component.html',
  styleUrl: './topheader.component.css'
})
export class TopheaderComponent {

}
